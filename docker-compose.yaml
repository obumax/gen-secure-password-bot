version: '3.8'
services:
  bot:
    build: .
    env_file:
      - .env
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - REDIS_URL=redis://:${REDIS_PASSWORD}@172.17.0.1:6379/0
    restart: always
